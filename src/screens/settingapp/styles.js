import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screen: {
    flex: 1,
  },
  text: { fontSize: 16, color: 'gray', marginTop: 10, marginLeft: 8 },
});
export default styles;
