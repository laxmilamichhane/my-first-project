import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screen: {
    flex: 1,
  },
  text: { fontSize: 18, color: 'gray', marginTop: 10, marginLeft: 8 },
});
export default styles;
