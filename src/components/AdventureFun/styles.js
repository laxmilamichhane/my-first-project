import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 40,
    height: 40,
    marginTop: 6,
  },
  name: {
    fontSize: 14,
    marginLeft: 8,
  },
});
export default styles;
