import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  type: {
    fontSize: 20,
    color: 'white',
    padding: 2,
  },
  container: {
    borderRadius: 26,
    borderWidth: 1,
    padding: 4,
    borderColor: 'white',
    margin: 8,
  },
});
export default styles;
