import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    fontSize: 18,
    color: 'white',
    marginBottom: 16,
  },
  images: {
    height: 26,
    width: 26,
  },
});

export default styles;
