import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: 'blue',
    borderRadius: 16,
    padding: 8,
    marginBottom: 8,
    marginHorizontal: 16,
  },
  text1: {
    textAlign: 'center',
    fontSize: 22,
  },
});
export default styles;
