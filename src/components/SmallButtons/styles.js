import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 26,
    width: 100,
    borderRadius: 10,
    marginTop: 30,
    alignSelf: 'center',
    marginHorizontal: 8,
  },
  name: {
    fontSize: 18,
    textAlign: 'center',
    color: 'white',
  },
});
export default styles;
