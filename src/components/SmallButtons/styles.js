import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 26,
    width: 100,
    borderRadius: 10,
    marginTop: 30,
    padding: 4,
    marginRight: 8,
    marginLeft: 8,
  },
  name: {
    fontSize: 16,
    textAlign: 'center',
  },
});
export default styles;
