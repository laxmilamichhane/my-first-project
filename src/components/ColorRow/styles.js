import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    marginRight: 10,
    padding: 28,
    marginTop: 14,
    marginBottom: 10,
    borderWidth: 1,
    borderRadius: 70,
    height: 10,
    width: 10,
  },
});

export default styles;
